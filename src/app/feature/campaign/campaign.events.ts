/**
 * TODO - Future proposed syntax for a module (e.g. campaigns) to listen and respond to general events
 *
 * E.g.
 * - evaluated shorthand `@campaigns` notation for templates
 * - declare and evaluate campaign methods (e.g mark as sent)
 *  */
