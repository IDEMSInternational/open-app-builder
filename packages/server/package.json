{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start": "docker-compose --file ./docker/docker-compose.yml --project-name plh_docker up",
    "stop": "docker-compose --file ./docker/docker-compose.yml --project-name plh_docker down",
    "dangerously_wipe": "docker-compose --file ./docker/docker-compose.yml -p plh_docker rm --volumes --force && docker volume prune"
  },
  "license": "ISC"
}
