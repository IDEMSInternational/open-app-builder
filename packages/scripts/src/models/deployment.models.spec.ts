import { extendDeploymentConfig, generateDeploymentConfig } from "../deployment.models";

describe("Deployment Models", () => {
  fit("Generates new deployment config", () => {
    expect(false).toEqual(true);
  });
  it("Extends deployment config", () => {});
});
