const download = (options: { src: string; target: string }) => null;
const filter = (options: { src: string; target: string }) => null;
const apply = (options: { src: string; target: string }) => null;

export default {
  // download, filter, apply
};
