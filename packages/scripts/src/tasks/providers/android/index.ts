import { configure } from "./configure";
import { generateAssets, generateIcon, generateSplash } from "./assets";

export default {
  configure,
  generateAssets,
  generateIcon,
  generateSplash,
};
