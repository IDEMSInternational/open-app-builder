import { configure } from "./configure";
import { generate_icon, generate_splash, generate_from_logo } from "./assets";

export default {
  configure,
  generate_icon,
  generate_splash,
  generate_from_logo,
};
