import {} from "./pipe";

const data_list_a = [
  {
    id: "id_1",
    first_name: "Alice",
    last_name: "Smith",
  },
  {
    id: "id_2",
    first_name: "Bob",
    last_name: "Jones",
  },
];

describe("Data Pipes Operators", () => {
  it("Filter (all)", () => {
    // validate
    // run
  });
  it("Filter (any)", () => {});
  it("Append Columns", () => {});
  it("Merge", () => {});
  it("Invalid Operation", () => {});
});

describe("Error Checking", () => {
  it("invalid args", () => {});
  it("missing source", () => {});
});

describe("Chained Data Pipe", () => {
  it("Filter (all)", () => {});
  it("Filter (any)", () => {});
  it("Append Columns", () => {});
  it("Merge", () => {});
  it("Chained operations");
});
