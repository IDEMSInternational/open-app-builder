# Testing the functionality of environment secrets in github
name: Environments Test
on:
  push:
    branches:
      - ci/enable-appetize-for-deployment-branches
jobs:
  environment-test:
    runs-on: ubuntu-latest
    environment: ${{ needs.steps.set_environment.outputs.my_env }}
    steps:
      - uses: actions/checkout@v3
        with:
          lfs: true
      - id: set_environment
        run: echo "::set-output name=my_env::wash"
      - name: Echo secret
        env:
            EXAMPLE_SECRET: ${{ secrets.EXAMPLE_SECRET }}
        run: |
          echo $EXAMPLE_SECRET

