import { IAppSkin } from "data-models";

const weekly_workshop: IAppSkin = {
  name: "weekly_workshop",
  appConfig: {
    APP_THEMES: {
      defaultThemeName: "default"
    }
  }
};

export default weekly_workshop;
